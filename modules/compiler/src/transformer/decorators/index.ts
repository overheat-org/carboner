import { Controller } from './controller';
import { HTTP } from './http';

export default { 
	Controller,
	Get: HTTP,
	Head: HTTP,
	Post: HTTP,
	Put: HTTP,
	Delete: HTTP,
	Connect: HTTP,
	Trace: HTTP,
	Patch: HTTP,
}